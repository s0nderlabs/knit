{
  "name": "knit-mcp",
  "version": "0.1.0",
  "description": "MCP server for Knit — Polkadot smart contract compilation, compatibility checking, and deployment tools",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@parity/resolc": "^1.0.0",
    "solc": "^0.8.28",
    "viem": "^2.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
